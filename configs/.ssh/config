# common settings for all hosts
Host *
    ServerAliveInterval 240
    SetEnv TERM=xterm-256color

# use SSH over the HTTPS port for GitHub
Host github.com
    Hostname ssh.github.com
    Port 443
    User git
