{"id":"muat71v10tlzrb","type":"fetch","timestamp":1789968779821,"urls":[{"url":"https://github.com/linuxdeepin/treeland/pull/1380","title":"linuxdeepin/treeland pull #1380: Fix virtual output","content":"#1380 Fix virtual output\n\n- type: pull\n- state: OPEN\n- author: Groveer\n- branch: master ← Groveer:fix_virtual_output\n- changes: +2558 −381, 28 files, 4 commits\n- created: 2026-09-07T10:13:49Z\n- labels: none\n- milestone: none\n- comments: 3; review threads: 23\n\n## Body\n# Fix virtual output（虚拟输出 / 复制模式修复）\n\n## 概述\n\n本 PR 解决两个层面的问题：\n\n1. **layer surface 生命周期缺陷**：复制/镜像模式下直接禁用 source 输出（如 HDMI-A-1 镜像 eDP-1 后禁用 eDP-1）时，复制模式折叠会销毁重建同一物理输出的 Output 包装对象（rewrap），旧实现把绑定在镜像输出上的 layer-shell 表面（如 dde-desktop 桌面）当作热插拔关闭，导致桌面窗口被销毁后回退为普通窗口。\n2. **热插拔/复制模式鲁棒性缺陷**：禁用/启用显示器、热插拔、复制模式下批处理输出时，窗口和光标乱跳、窗口搁浅在被禁用的屏幕上、过期的存储配置恢复出残缺的复制组。\n\n共 4 笔提交，依赖顺序：协议层 v3 通知（提交 1）→ 服务端复制模式/热插拔行为（提交 2）→ 交互验证工具（提交 3）→ 客户端空事件语义（提交 4）。\n\n---\n\n## 提交 1：fix(virtual-output): harden protocol layer\n\n把 treeland_virtual_output_manager_v1 从「v1 一次性创建」升级为「v3 动态通知 + 完整生命周期」。\n\n### InterfaceVersion 1 → 3\nWayland 全局对象只能绑定到 min(全局版本, 客户端支持版本)。版本为 1 时，destroy 请求（since 2）与 virtual_output_modified 事件（since 3）在协议层不可达。升级版本以开通 v2/v3 通道，是所有通知功能的前提。\n\n### 子资源版本（createVirtualOutputInternal）\n子资源 new_id 直接以 resource->version() 创建，与代码库其他协议模块（foreign-toplevel、wallpaper 等）的写法保持一致；子接口 treeland_virtual_output_v1 的接口版本（v1）由 treeland-protocols 的协议 XML 定义，本实现不做额外钳制。\n\n### attachDestroyCleanup 销毁路径通知\n组被销毁（客户端 destroy 或断开）也是配置集合的变化。beforeDestroy 里移除配置后广播 virtual_output_modified，让其他 v3 客户端感知「组已消失」并刷新列表。\n\n### notifyVirtualOutputModified（新增）\n遍历所有 manager 绑定，只对 version ≥ 3 的绑定发送 virtual_output_modified。生成的 sender 不做版本检查，给 v1/v2 绑定发 since-3 事件会让不认识该 opcode 的客户端直接协议错误断开，显式按版本过滤是唯一安全做法。\n\n### storeVirtualOutput 存储后通知\n新建组 / 恢复配置后广播 modified，其他客户端及时重拉列表。\n\n### manager destroy() 重写恢复\n协议 request destroy 进入生成的 handle_destroy，基类默认实现为空操作、不会销毁资源，只有 wl_resource_destroy 才触发 destroy_func 并从 resourceMap 摘除。外部协议包补上 since-2 的 destroy 请求后恢复该重写，客户端主动销毁时资源立即释放，避免挂起到断线才回收的泄漏与悬挂。\n\n### get_virtual_output：已消失组返回空代理\n原来组不存在时用 wl_resource_post_error 返回致命协议错误（杀死整个客户端连接）；「列表后组消失」是合法竞态（如列表下发后复制组被热插拔塌缩），不应惩罚客户端。改为仍创建子资源并发送空 outputs 事件，语义为「请重新查询」。\n\n### get_virtual_output：QPointer 重绑 + 销毁守卫\n配置按设计不随创建者断开而消失，其 QPointer 若停在旧代理上，后续更新会推给空壳。get_virtual_output 每次把配置 QPointer 重绑到最新代理，保证「每次查询的最新绑定」接管后续推送。配套在 attachDestroyCleanup 的清理 lambda 中捕获代理指针，仅当配置仍绑定在被销毁的代理上时才移除（it != m_configs.end() && it.value().virtualOutput == virtualOutput），否则静默跳过——只有一个 name 一个活跃绑定槽位，无条件 remove 会把「代理销毁」误当「组被移除」，误删已重绑新代理的配置。注意必须用迭代器判断而非 contains()+value()：QHash::value 对缺失键返回默认构造的配置（virtualOutput 为空），会与「绑定被拒/从未绑定」混为一谈。\n\n### updateVirtualOutput / removeVirtualOutput 追加通知\n更新 outputs 并推送后广播 modified；remove 改为仅当移除成功（配置存在）才通知。helper 的 copyOutputConfigurationChanged 在这两处驱动复制模式的热更新与删除（hotplug 事件入口），UI 客户端无需重连即可刷新。\n\n---\n\n## 提交 2：fix(output): robust hotplug restore and copy mode handling\n\n核心提交，两条功能线：热插拔/复制模式鲁棒性 + layer surface 生命周期修复。\n\n### ① WOutputLayout::pin（waylib）\n新增 pin(WOutput*)：对仍处于 auto_configured 的输出，用 wlr_output_layout_add 以当前坐标重新添加，清除 auto 标记、坐标转为「手动冻结」。wlroots 对 auto 输出会在兄弟屏几何变化时触发 output_layout_reconfigure 整体重排，悄悄挪动已排定的位置；pin 后热插拔/改分辨率不再导致布局跳动。\n\n### ② 位置恢复去重叠（helper.cpp 静态助手 + 4 个应用点）\neffectiveOutputLayoutSize 按 transform 交换宽高、按 scale 折算模拟 wlroots 布局尺寸；enabledStatesOverlap 检测恢复拓扑中任意两个 enabled 输出在布局坐标下相交；deOverlapEnabledStates 在扩展模式提交前把重叠状态贪心平移到最右边缘之后（兜住客户端用 (0,0) 恢复屏幕）；savedPositionOverlapsEnabledOutput 恢复单输出保存位置前与当前已启用输出比对（用 wlr_output_layout_get_box 取真实矩形）。\n\n应用点：onOutputAdded（恢复保存拓扑）、onOutputTestOrApply 扩展模式恢复分支、onOutputTestOrApply 提交尾部、restoreExtensionModeFromConfig。统一策略：过期保存坐标与当前拓扑重叠即放弃恢复，避免叠屏塌缩与窗口/光标乱跳。\n\n### ③ 复制模式转换（helper.cpp）\n- **handleCopyModeSourceDisabled（替代旧 handleCopyModeOutputDisable）**：不再无条件塌缩成扩展模式。先统计存活镜像数（排除自身、同批禁用、已禁用），少于 2 个才返回 false 走塌缩；否则提升新复制源并重定向其余镜像。禁用单个镜像不等于复制模式损坏（重启用时通过有效代理自动恢复镜像），只有禁用 source 才必须重选源。重建后的新组不持久化（见⑤），保留客户端的原始复制配置，重新 enable 原 source 时按其恢复。\n- **reEnabledCopySource 恢复**：onOutputTestOrApply 中检测重新启用的输出是否等于 dconfig 复制组首个成员（原 source）：是则恢复原始复制组（Copy 模式走 applyCopyModeToOutputs、Extension 模式走 restoreConfiguredCopyMode），而不是塌缩成扩展模式。\n- **promoteCopyOutputToSource**：从 onOutputRemoved 抽出的共用逻辑——把选中镜像换装为新 source、设为主屏、迁移源上搁浅的窗口、重建其余镜像。消除两套相似代码，镜像提升/源拔出行为统一。\n- **convertCopyOutputsToNormal(skip) / convertCopyModeToExtension(preservedOutput)**：把非 source 的复制输出全部转回独立输出，skip 谓词约束哪些不参与（被禁用的源保留为 disabled normal 输出）；混合拓扑（镜像+独立屏）塌成扩展模式。\n- **onOutputTestOrApply 复制模式分支**：仅当拓扑变化（enabled 翻转）才处理：禁用 source 走提升路径；启用独立屏（source 被启用但非 primary）且非配置中的原\n\n[body truncated; use get_search_content findText or offset for the full text]\n\n## Checks\nNo check rollup data available.\n\n## Review verdicts\n- sourcery-ai: COMMENTED — Sorry @Groveer, your pull request is larger than the review limit of 150,000 diff characters\n- Groveer: COMMENTED\n- zzxyb: COMMENTED\n- copilot-pull-request-reviewer: COMMENTED — <!-- ccr-overview-v2 -->  ## Copilot review overview  ### 🟡 Changes recommended  Several correctness issues remain in proxy lifetime and refresh handling, de-overlapped state persistence, and output-mode event ordering.  *Get a fresh assessment by requesting another Copilot review.*  **Review effor  [review by copilot-pull-request-reviewer truncated; use get_search_content findText or offset for the full text]\n\n## Linked references\nUnavailable in this GitHub fetch path; use gh for linked references.\n\n## Files\n- examples/test_virtual_output/CMakeLists.txt (+4/−0)\n- examples/test_virtual_output/main.cpp (+2/−10)\n- examples/test_virtual_output/virtualclient.cpp (+771/−77)\n- examples/test_virtual_output/virtualclient.h (+103/−16)\n- examples/test_virtual_output/virtualoutput.cpp (+17/−13)\n- examples/test_virtual_output/virtualoutputmanager.cpp (+17/−9)\n- examples/test_virtual_output/virtualoutputmanager.h (+2/−0)\n- examples/test_virtual_output/wlroutputmanager.cpp (+315/−0)\n- examples/test_virtual_output/wlroutputmanager.h (+67/−0)\n- src/core/layersurfacecontainer.cpp (+30/−1)\n- src/core/layersurfacecontainer.h (+8/−0)\n- src/core/rootsurfacecontainer.cpp (+24/−6)\n- src/core/rootsurfacecontainer.h (+11/−2)\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp (+42/−21)\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.h (+1/−1)\n- src/output/outputmanager.cpp (+9/−1)\n- src/seat/helper.cpp (+483/−138)\n- src/seat/helper.h (+21/−1)\n- src/surface/surfacecontainer.cpp (+5/−0)\n- src/surface/surfacecontainer.h (+7/−1)\n- tests/protocols/INDEX.md (+2/−2)\n- tests/protocols/treeland-virtual-output-desktop-v1/CMakeLists.txt (+3/−1)\n- tests/protocols/treeland-virtual-output-desktop-v1/README.md (+47/−37)\n- tests/protocols/treeland-virtual-output-desktop-v1/setup.cpp (+38/−0)\n- tests/protocols/treeland-virtual-output-desktop-v1/treeland-virtual-output-desktop-v1.c (+495/−44)\n- tests/protocols/treeland-virtual-output-desktop-v1/treeland-virtual-output-desktop-v1.h (+7/−0)\n- waylib/src/server/kernel/woutputlayout.cpp (+21/−0)\n- waylib/src/server/kernel/woutputlayout.h (+6/−0)\n\n## Commits\n- 97ab704 fix(virtual-output): harden protocol layer\n- 45f3744 fix(output): robust hotplug restore and copy mode handling\n- 8965aeb feat(test-virtual-output): extend example client\n- 0f387f6 fix(virtual-output): emit empty outputs events for vanished groups\n\n## Conversation comments\n- deepin-ci-robot at 2026-09-07T10:14:03Z:\n  [APPROVALNOTIFIER] This PR is **NOT APPROVED**\n  \n  This pull-request has been approved by: *<a href=\"https://github.com/linuxdeepin/treeland/pull/1380#\" title=\"Author self-approved\">Groveer</a>*\n  \n  \n  The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=linuxdeepin%2Ftreeland).\n  \n  <details open>\n  Needs approval from an approver in each of these files:\n  \n  - **[debian/deepin/OWNERS](https://github.com/linuxdeepin/treeland/blob/master/debian/deepin/OWNERS)**\n  \n  Approvers can indicate their approval by writing `/approve` in a comment\n  Approvers can cancel approval by writing `/approve cancel` in a comment\n  </details>\n  <!-- META={\"approvers\":[]} -->\n- sourcery-ai at 2026-09-07T10:14:23Z:\n  <!-- Generated by sourcery-ai[bot]: start review_guide -->\n  \n  ## Reviewer's Guide\n  \n  The pull request turns the virtual-output example into a full interactive output-management client and expands the virtual-output protocol, while hardening compositor output topology transitions, layout positioning, cursor placement, primary-output persistence, and surface restoration across disable/enable and copy-mode changes.\n  \n  #### Sequence diagram for copy-source disable and restoration\n  \n  ```mermaid\n  sequenceDiagram\n      participant Client as OutputManagementClient\n      participant Helper\n      participant RootSurfaceContainer\n      participant OutputManager\n      participant Wlroots\n  \n      Client->>Helper: onOutputTest\n  \n  [comment IC_kwDOFuYCn88AAAABS_F3qw truncated; use get_search_content findText or offset for the full text]\n- deepin-bot at 2026-09-10T11:49:38Z:\n  **TAG Bot**\n  \n  New tag: 0.10.0\n  DISTRIBUTION: unstable\n  Suggest: synchronizing this PR through rebase #1392\n\n## Review thread comments\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:265 — sourcery-ai[bot] (resolution state unknown):\n  **issue (bug_risk):** Rebinding `m_configs[name].virtualOutput` to the newest proxy does not make the destroy cleanup proxy-specific: when the older proxy is later destroyed, its `beforeDestroy` handler unconditionally removes `m_configs[name]`, including the configuration now bound to the newer proxy.\n  \n  **Triggers:** When a client calls `get_virtual_output` for an existing group more than once and then destroys the older proxy.\n  \n  **Suggested fix:** Capture the proxy in the cleanup callback and remove the config only when `m_configs[name].virtualOutput` still points to that proxy.\n  \n  ✅ Addressed in 576291721a650342d72350e5e14cd2c2044b35db: The cleanup callback now captures the specific proxy and\n  \n  [review thread comment 3948717932 truncated; use get_search_content findText or offset for the full text]\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:267 — sourcery-ai[bot] (resolution state unknown):\n  **issue (bug_risk):** The vanished-group path sends an empty outputs array, but the example client's `VirtualOutput::treeland_virtual_output_v1_outputs` handler returns without emitting `outputsReceived` when the array is empty, so the client never observes the required empty outputs event.\n  \n  **Triggers:** When a listed virtual-output group disappears before the client calls `get_virtual_output`.\n  \n  **Suggested fix:** Emit `outputsReceived(name, {})` for an empty array instead of returning before the signal emission.\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:265 — Groveer (resolution state unknown):\n  感谢审查,这个问题属实,已修复。\n  \n  **问题确认**:`get_virtual_output` 会把 `m_configs[name].virtualOutput` 重绑到最新创建的 proxy,但 `attachDestroyCleanup` 中旧的清理 lambda 无条件执行 `m_configs.remove(name)`,导致旧 proxy 销毁时误删仍绑定在新 proxy 上的配置(nit:清理回调也删除了 notifyVirtualOutputModified 的触发条件)。\n  \n  **修复方式**(`attachDestroyCleanup`):清理 lambda 现在捕获 `virtualOutput` 指针,只有在 `m_configs[name].virtualOutput == virtualOutput` 时才移除配置并通知,新绑定不受旧 proxy 销毁影响。\n  \n  已在 commit `57629172` 中修复并推送。\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:267 — Groveer (resolution state unknown):\n  感谢审查,这个问题属实,已修复。\n  \n  **问题确认**:服务端 vanished-group 路径发送空 outputs 数组,但示例客户端 `treeland_virtual_output_v1_outputs` handler 在数组为空时直接 `return`,不发出 `outputsReceived` 信号,客户端无法感知组已消失。\n  \n  **修复方式**(`virtualoutput.cpp`):handler 现在对空数组也统一发出 `outputsReceived(name, {})`,客户端能观察到空输出事件,`onVirtualOutputOutputs` 会正确清空该组的镜像映射。\n  \n  已在 commit `57629172` 中修复并推送。\n- src/core/layersurfacecontainer.cpp:159 — zzxyb (resolution state unknown):\n  这个closed会直接导致客户端的layershell走close逻辑，但错误可能只出现在treeland侧而不是客户端侧，没明白这个修改的目的是什么\n- src/core/layersurfacecontainer.cpp:83 — zzxyb (resolution state unknown):\n  看着这个函数是多余的，其逻辑应该对现有的 void removeOutput(Output *output) override;进行扩展\n- src/core/rootsurfacecontainer.cpp:204 — zzxyb (resolution state unknown):\n  建议跟removeoutput逻辑融合，简单些\n- src/core/rootsurfacecontainer.cpp:428 — zzxyb (resolution state unknown):\n  原来的逻辑没有问题。但是这个新的逻辑会导致就算没有更新主屏幕，如果updateDconfig为true会强行调用更新一下主屏幕，这个逻辑是为什么？\n- src/core/rootsurfacecontainer.cpp:438 — zzxyb (resolution state unknown):\n  这里的意思是鼠标如果在屏幕上就return,不在就移到主屏幕的中间去。原逻辑是一个fallback处理，修改后fallback处理成了移到靠近的屏幕中间去，这个是否符合产品行为要考虑清楚，你得到的x、y不一定在中间\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:131 — zzxyb (resolution state unknown):\n  这个删除掉，否则会引起兼容问题\n- src/core/layersurfacecontainer.cpp:159 — Groveer (resolution state unknown):\n  这个 `closed()` 是把原来的 `Q_ASSERT(container)`（debug 直接 abort，release 下 `Q_ASSERT` 被编译掉后对空指针继续调用 `container->addSurface()` 导致解引用崩溃）换成了兜底处理。\n  \n  触发场景是\"最后一个输出被移除\"：`RootSurfaceContainer::removeOutput` 里先摘掉布局、切换主屏（布局已空时主屏切换分支不执行，primary 仍指向被移除的输出），随后 `LayerSurfaceContainer::removeOutput` 迁移无输出绑定的全局层表面（如 dde-desktop）时 `addSurfaceToContainer` 查不到该输出的 per-output 容器。此时按 layer-shell 协议惯例，输出被销毁后合成器发 `closed`、客户端销毁重建，是规范行为（不是客户端协议错误）。\n  \n  正常复制折叠/包装交换路径不会走到这里——那部分走 `detachOutputSurfaces`（摘而不关），物理输出还在，绝不发 closed。\n  \n- src/core/layersurfacecontainer.cpp:83 — Groveer (resolution state unknown):\n  `detachOutputSurfaces` 和 `removeOutput` 语义不同，不宜并入：\n  \n  - `removeOutput` 处理的是**物理输出被摘除**：无输出绑定的全局层表面迁到新主屏（`addSurfaceToContainer`），其余 `closed()`，最后销毁容器。\n  - `detachOutputSurfaces` 处理的是**同一物理输出更换 Output 包装对象**（复制模式折叠/展开的 rewrap）：表面必须摘下来保持存活（不 close），由新包装 `addOutput` 时 `updateSurfacesContainer()` 重新进入新容器。\n  \n  两者合并会让\"摘而不关\"和\"关闭/迁移\"两种语义混在一个函数里，反而重新引入原 bug：复制折叠把绑定镜像输出的桌面层表面当成热插拔关闭（closed → dde-desktop 回退普通窗口）。这个缺陷正是本 PR 要修的核心问题之一，所以拆开是刻意的。\n  \n- src/core/rootsurfacecontainer.cpp:204 — Groveer (resolution state unknown):\n  `detachLayerSurfaces` 只是对 4 个 layer 层级的 `LayerSurfaceContainer` 遍历调用 `detachOutputSurfaces`，是 rewrap 路径（`createNormalOutput`/`createCopyOutput` 换包装前）的统一入口；`removeOutput` 是物理摘除路径。二者触发点不同、语义不同（理由同 layersurfacecontainer 那个评论），合并会混淆\"换包装摘而不关\"与\"物理摘除关闭/迁移\"，所以保持分离。\n  \n- src/core/rootsurfacecontainer.cpp:428 — Groveer (resolution state unknown):\n  这里去掉 early-return 是为了堵\"禁用/启用循环后主屏持久化丢失\"：\n  \n  场景：dconfig 持久化的主屏是 X → 禁用 X 后恢复逻辑把主屏切到 Y 并回写 dconfig → 重新启用 X 时 `restoreScreenAsPrimary` 再次恢复 X。此时内存里 `m_primaryOutput` 可能已经是 X（上一轮恢复的残留），而持久化 dconfig 仍是 Y。旧代码 `if (m_primaryOutput == newPrimaryOutput) return;` 会把这次 `setPrimaryOutputConfig(X)` 的回写丢掉，主屏设定跨循环停在 Y，与内存不一致。\n  \n  新逻辑：`updateDconfig=true` 时无条件回写（`setPrimaryOutputConfig` 写入幂等，值相同等于没写），保证禁用/启用循环后持久化与内存收敛。\n  \n- src/core/rootsurfacecontainer.cpp:438 — Groveer (resolution state unknown):\n  已按你的意见回退：`ensureCursorVisible` 恢复原逻辑——光标不在任何输出上时 warp 到主屏中心（`m_primaryOutput->geometry().center()`），`wlr_output_layout_closest_point` 的改动已撤销。\n  \n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:131 — Groveer (resolution state unknown):\n  已按你的意见删除 `std::min` 版本钳制，子资源直接传 `resource->version()`，与代码库其他模块（foreign-toplevel、wallpaper 等）的写法保持一致。\n  \n- src/core/rootsurfacecontainer.cpp:428 — zzxyb (resolution state unknown):\n  dconfig只有在控制中心这种设置才能更新dconfig，你所说的场景不应该更新dconfig主屏配置\n- src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp:148 — Copilot (resolution state unknown):\n  EN: Rebinding `m_configs[name].virtualOutput` protects only the map removal below; the first `beforeDestroy` connection still emits `destroyVirtualOutput` for every proxy. If a client re-queries a group and then destroys its old proxy, that stale proxy calls `Helper::onRestoreCopyOutput()` and tears down the active copy mode even though the config is bound to the newer proxy. Gate this signal with the same iterator/pointer check.\n  中文：这里的重绑定只保护了下面的配置移除逻辑，但前一个 `beforeDestroy` 连接仍会对每个代理发送 `destroyVirtualOutput`。客户端重新查询后销毁旧代理时，旧代理仍会调用 `Helper::onRestoreCopyOutput()`，误拆除仍由新代理绑定的有效复制模式。应使用相同的迭代器/指针判断保护该信号。\n- examples/test_virtual_output/virtualclient.cpp:393 — Copilot (resolution state unknown):\n  EN: Refreshing only the virtual-output name list is insufficient for an existing group. The server stores one `virtualOutput` proxy per config and sends updated member lists only to that proxy; other clients receive `virtual_output_modified` but their existing names remain in `m_virtualOutputs`, so `onVirtualOutputListReceived()` skips `getVirtualOutput()` and their mirror mappings never update. Re-query/rebind each existing group named by the refreshed list (or otherwise fetch its outputs) when handling the modified event.\n  中文：只刷新虚拟输出名称列表不足以更新已存在的复制组。服务端每个配置只保存一个 `virtualOutput` 代理，成员列表更新事件只发送给该代理；其他客户端虽然收到 `virtual_output_modified`，但名称仍在 `m_virtualOutputs` 中，因此 `onVirtualOutputListReceived(\n  \n  [review thread comment 4059159246 truncated; use get_search_content findText or offset for the full text]\n- examples/test_virtual_output/virtualclient.cpp:744 — Copilot (resolution state unknown):\n  EN: When reconciling a vanished group, erasing the `QSharedPointer` only invokes `VirtualOutput::~VirtualOutput()`, whose body does not send the generated Wayland `destroy()` request. The child resource remains alive on the server until disconnect; explicitly destroy the proxy before erasing it.\n  中文：对消失组做对账时，擦除 `QSharedPointer` 只会调用 `VirtualOutput::~VirtualOutput()`，其函数体不会发送生成代理的 Wayland `destroy()` 请求。子资源会一直留在服务端直到断开连接；应在擦除前显式销毁代理。\n  \n  This issue also appears on line 775 of the same file.\n- examples/test_virtual_output/virtualclient.cpp:825 — Copilot (resolution state unknown):\n  EN: The server can reject this request (for example, when the selected mirror is already in another copy group or the name is duplicated), but `createVirtualOutput()` still returns a proxy and this block immediately inserts it into every local map without observing `VirtualOutput::errorOccurred`. The UI then reports a mirror group that was never created and can issue further operations against it. Connect the error signal and roll back the maps, or validate conflicts before inserting.\n  中文：服务端可能拒绝此请求（例如所选镜像已属于其他复制组，或组名重复），但 `createVirtualOutput()` 仍会返回代理，本段在未处理 `VirtualOutput::errorOccurred` 的情况下就把它加入所有本地映射。UI 随后会显示一个实际未创建的复制组，并可能继续对其操作。应连接错误信号回滚映射，或在插入前校验冲突。\n- examples/test_virtual_output/wlroutputmanager.cpp:109 — Copilot (resolution state unknown):\n  EN: The protocol does not guarantee event order, so `current_mode` can arrive before the corresponding `mode` declaration. Then this lookup never finds the object and `currentModeIndex` remains `-1` after `done`; the UI omits the current mode/refresh when re-applying other heads. Store the current mode object and resolve it after all declarations, or update the index when modes are added.\n  中文：该协议不保证事件顺序，因此 `current_mode` 可能先于对应的 `mode` 声明到达。此时查找不会命中对象，`done` 后 `currentModeIndex` 仍为 `-1`；UI 在重新应用其他输出时会遗漏当前模式/刷新率。应保存当前模式对象并在所有声明完成后解析，或在新增模式时更新索引。\n- src/seat/helper.cpp:1368 — Copilot (resolution state unknown):\n  EN: `m_pendingOutputConfig.states` was copied before this block mutates `states`. The de-overlapped positions are used for layout and commits, while `onOutputCommitFinished()` and `sendResult()` still consume the old overlapping list; persisted/client-reported coordinates can immediately recreate the stale topology. Update the pending copy after `deOverlapEnabledStates()`.\n  中文：`m_pendingOutputConfig.states` 在本段修改 `states` 之前就完成了复制。布局和提交使用去重叠后的坐标，但 `onOutputCommitFinished()` 与 `sendResult()` 仍使用旧的重叠列表，持久化和客户端回报的坐标会立即重新造成过期拓扑。应在 `deOverlapEnabledStates()` 之后更新 pending 副本。\n\n## Availability notes\n- Some GitHub fields were unavailable from this gh version; retried with the core field set.\n\n## Appendix\n## Full body\n\n# Fix virtual output（虚拟输出 / 复制模式修复）\n\n## 概述\n\n本 PR 解决两个层面的问题：\n\n1. **layer surface 生命周期缺陷**：复制/镜像模式下直接禁用 source 输出（如 HDMI-A-1 镜像 eDP-1 后禁用 eDP-1）时，复制模式折叠会销毁重建同一物理输出的 Output 包装对象（rewrap），旧实现把绑定在镜像输出上的 layer-shell 表面（如 dde-desktop 桌面）当作热插拔关闭，导致桌面窗口被销毁后回退为普通窗口。\n2. **热插拔/复制模式鲁棒性缺陷**：禁用/启用显示器、热插拔、复制模式下批处理输出时，窗口和光标乱跳、窗口搁浅在被禁用的屏幕上、过期的存储配置恢复出残缺的复制组。\n\n共 4 笔提交，依赖顺序：协议层 v3 通知（提交 1）→ 服务端复制模式/热插拔行为（提交 2）→ 交互验证工具（提交 3）→ 客户端空事件语义（提交 4）。\n\n---\n\n## 提交 1：fix(virtual-output): harden protocol layer\n\n把 treeland_virtual_output_manager_v1 从「v1 一次性创建」升级为「v3 动态通知 + 完整生命周期」。\n\n### InterfaceVersion 1 → 3\nWayland 全局对象只能绑定到 min(全局版本, 客户端支持版本)。版本为 1 时，destroy 请求（since 2）与 virtual_output_modified 事件（since 3）在协议层不可达。升级版本以开通 v2/v3 通道，是所有通知功能的前提。\n\n### 子资源版本（createVirtualOutputInternal）\n子资源 new_id 直接以 resource->version() 创建，与代码库其他协议模块（foreign-toplevel、wallpaper 等）的写法保持一致；子接口 treeland_virtual_output_v1 的接口版本（v1）由 treeland-protocols 的协议 XML 定义，本实现不做额外钳制。\n\n### attachDestroyCleanup 销毁路径通知\n组被销毁（客户端 destroy 或断开）也是配置集合的变化。beforeDestroy 里移除配置后广播 virtual_output_modified，让其他 v3 客户端感知「组已消失」并刷新列表。\n\n### notifyVirtualOutputModified（新增）\n遍历所有 manager 绑定，只对 version ≥ 3 的绑定发送 virtual_output_modified。生成的 sender 不做版本检查，给 v1/v2 绑定发 since-3 事件会让不认识该 opcode 的客户端直接协议错误断开，显式按版本过滤是唯一安全做法。\n\n### storeVirtualOutput 存储后通知\n新建组 / 恢复配置后广播 modified，其他客户端及时重拉列表。\n\n### manager destroy() 重写恢复\n协议 request destroy 进入生成的 handle_destroy，基类默认实现为空操作、不会销毁资源，只有 wl_resource_destroy 才触发 destroy_func 并从 resourceMap 摘除。外部协议包补上 since-2 的 destroy 请求后恢复该重写，客户端主动销毁时资源立即释放，避免挂起到断线才回收的泄漏与悬挂。\n\n### get_virtual_output：已消失组返回空代理\n原来组不存在时用 wl_resource_post_error 返回致命协议错误（杀死整个客户端连接）；「列表后组消失」是合法竞态（如列表下发后复制组被热插拔塌缩），不应惩罚客户端。改为仍创建子资源并发送空 outputs 事件，语义为「请重新查询」。\n\n### get_virtual_output：QPointer 重绑 + 销毁守卫\n配置按设计不随创建者断开而消失，其 QPointer 若停在旧代理上，后续更新会推给空壳。get_virtual_output 每次把配置 QPointer 重绑到最新代理，保证「每次查询的最新绑定」接管后续推送。配套在 attachDestroyCleanup 的清理 lambda 中捕获代理指针，仅当配置仍绑定在被销毁的代理上时才移除（it != m_configs.end() && it.value().virtualOutput == virtualOutput），否则静默跳过——只有一个 name 一个活跃绑定槽位，无条件 remove 会把「代理销毁」误当「组被移除」，误删已重绑新代理的配置。注意必须用迭代器判断而非 contains()+value()：QHash::value 对缺失键返回默认构造的配置（virtualOutput 为空），会与「绑定被拒/从未绑定」混为一谈。\n\n### updateVirtualOutput / removeVirtualOutput 追加通知\n更新 outputs 并推送后广播 modified；remove 改为仅当移除成功（配置存在）才通知。helper 的 copyOutputConfigurationChanged 在这两处驱动复制模式的热更新与删除（hotplug 事件入口），UI 客户端无需重连即可刷新。\n\n---\n\n## 提交 2：fix(output): robust hotplug restore and copy mode handling\n\n核心提交，两条功能线：热插拔/复制模式鲁棒性 + layer surface 生命周期修复。\n\n### ① WOutputLayout::pin（waylib）\n新增 pin(WOutput*)：对仍处于 auto_configured 的输出，用 wlr_output_layout_add 以当前坐标重新添加，清除 auto 标记、坐标转为「手动冻结」。wlroots 对 auto 输出会在兄弟屏几何变化时触发 output_layout_reconfigure 整体重排，悄悄挪动已排定的位置；pin 后热插拔/改分辨率不再导致布局跳动。\n\n### ② 位置恢复去重叠（helper.cpp 静态助手 + 4 个应用点）\neffectiveOutputLayoutSize 按 transform 交换宽高、按 scale 折算模拟 wlroots 布局尺寸；enabledStatesOverlap 检测恢复拓扑中任意两个 enabled 输出在布局坐标下相交；deOverlapEnabledStates 在扩展模式提交前把重叠状态贪心平移到最右边缘之后（兜住客户端用 (0,0) 恢复屏幕）；savedPositionOverlapsEnabledOutput 恢复单输出保存位置前与当前已启用输出比对（用 wlr_output_layout_get_box 取真实矩形）。\n\n应用点：onOutputAdded（恢复保存拓扑）、onOutputTestOrApply 扩展模式恢复分支、onOutputTestOrApply 提交尾部、restoreExtensionModeFromConfig。统一策略：过期保存坐标与当前拓扑重叠即放弃恢复，避免叠屏塌缩与窗口/光标乱跳。\n\n### ③ 复制模式转换（helper.cpp）\n- **handleCopyModeSourceDisabled（替代旧 handleCopyModeOutputDisable）**：不再无条件塌缩成扩展模式。先统计存活镜像数（排除自身、同批禁用、已禁用），少于 2 个才返回 false 走塌缩；否则提升新复制源并重定向其余镜像。禁用单个镜像不等于复制模式损坏（重启用时通过有效代理自动恢复镜像），只有禁用 source 才必须重选源。重建后的新组不持久化（见⑤），保留客户端的原始复制配置，重新 enable 原 source 时按其恢复。\n- **reEnabledCopySource 恢复**：onOutputTestOrApply 中检测重新启用的输出是否等于 dconfig 复制组首个成员（原 source）：是则恢复原始复制组（Copy 模式走 applyCopyModeToOutputs、Extension 模式走 restoreConfiguredCopyMode），而不是塌缩成扩展模式。\n- **promoteCopyOutputToSource**：从 onOutputRemoved 抽出的共用逻辑——把选中镜像换装为新 source、设为主屏、迁移源上搁浅的窗口、重建其余镜像。消除两套相似代码，镜像提升/源拔出行为统一。\n- **convertCopyOutputsToNormal(skip) / convertCopyModeToExtension(preservedOutput)**：把非 source 的复制输出全部转回独立输出，skip 谓词约束哪些不参与（被禁用的源保留为 disabled normal 输出）；混合拓扑（镜像+独立屏）塌成扩展模式。\n- **onOutputTestOrApply 复制模式分支**：仅当拓扑变化（enabled 翻转）才处理：禁用 source 走提升路径；启用独立屏（source 被启用但非 primary）且非配置中的原 source 时塌成扩展；纯刷新率/模式切换不视为拓扑变化、保持复制模式。旧代码无条件把复制输出全转普通输出，一次刷新切换就拆散镜像组。\n\n### ④ 提交回调健壮化\n抽出公共 syncLayoutAndRefresh lambda（布局增删 + state-only commit 强制重渲染），两个分支共用防行为漂移。committedState 与 extraState 不一致时不再一律判失败，而是对照输出真实硬件状态逐项比对（enabled 方向、mode 尺寸/刷新率、scale、transform），匹配即按成功处理。复制模式塌缩会为幸存镜像并发重建新 Output，其初始化 commit 顶替已排定状态是正常现象而非失败；但仅比对方向会掩盖丢掉的刷新率/模式变更，导致持久化配置与硬件脱节。\n\n### ⑤ 持久化时机（treeland 不写 dconfig）\n核心原则：**dconfig 只在客户端（virtual-output 协议 / 控制中心）设置时写入**，treeland 运行时（disable/enable、复制源提升/塌缩、热插拔恢复）一律不写。具体：\n- 删除 pending copyOutputIds 机制——source-disable 提升/塌缩不再持久化新复制组，客户端原始配置原样保留，重新 enable 原 source 时据此恢复原组。\n- onOutputCommitFinished 不再在扩展模式 apply 成功后清空复制配置（纯刷新/塌缩不丢配置）。\n- restoreScreenAsPrimary 不再显式回写主屏 dconfig；主屏恢复仅走内存（enable 原 source 时由恢复路径 setPrimaryOutput）。\n- storeSingleOutputConfig 增加守卫：复制配置激活（createCopyOutput）时跳过，防止塌缩后只剩幸存屏时把 singleOutputId 写成幸存屏而污染 shouldRestoreCopyMode，导致原 source 无法恢复。\n- copyModeRestoreConfig 守卫改为 id 数量与 name 数量必须相等（旧逻辑只看 <2，id 失效会静默恢复出残缺组），拒绝恢复并告警。\n\n### ⑥ 搁浅窗口恢复\n新增 m_disabledOutputSurfaces（输出 id → 表面列表 + 绝对坐标快照，QPointer 防止禁用期间窗口被关导致悬垂）。每次 enabled 翻转禁用都重新拍快照（上次失败提交的残留位置不可信）。提交成功路径：扩展模式下还原表面位置、重挂 ownsOutput 并刷新输出归属，任何模式都清理条目（长期禁用不泄漏）；复制模式不还原（复制逻辑自持放置权）。getOutputAtCursor 跳过未启用的输出（禁用屏的 outputItem 保留旧几何，命中它会把交互/弹窗吸到死屏）；ensureCursorVisible 改为 public 并在 applyCopyModeToOutputs 末尾调用：恢复复制组/启用原 source 后布局重排，光标若落在屏幕外自动 warp 回主屏中心（仅当光标不在任何输出上才触发，无副作用）。\n\n### ⑦ layer surface 生命周期（核心）\n**机制**：RootSurfaceContainer → 4 个按 layer 层级的 LayerSurfaceContainer → 每输出一个 OutputLayerSurfaceContainer。layer surface 通过 doAddSurface(surface, true) 把 QObject parent 与 QQuickItem parentItem 都挂到 per-output 容器；容器被 deleteLater 时 Qt 级联销毁所有子 QObject，未走 destroy() 的 SurfaceWrapper 析构会 invalidate 拆掉 shell surface——容器销毁即表面销毁。关闭链：WLayerSurface::closed（zwlr_layer_surface_v1.closed）→ ShellHandler → dde-desktop 收到 closed 后重建，作为输出绑定的层表面表现为回退普通窗口。\n\n**旧 bug 根因**：复制模式折叠对同一物理 WOutput 删除再重建 Output 包装对象（rewrap），旧实现把「绑定过具体输出」的表面一律 closed()，把包装交换误当物理摘除。\n\n**修复**：\n- **detachOutputSurfaces(Output*)**：三步摘下而不关闭——① container->removeSurface（清容器归属、setOutputs({})，保留 QObject parent）；② unparentSurface（卸掉 QObject parent 与 parentItem，防止旧容器销毁时连带销毁表面）；③ doAddSurface(surface, false) 无容器挂回该 layer 自身 model，供新包装 addOutput 时经 updateSurfacesContainer 自动 re-enter。remove/re-add 在同一事件循环内完成、中间不跑事件循环，监听者只见瞬态，永不触发 closed。\n- **detachLayerSurfaces(Output*)**：一个物理输出在 4 个层级各有一个 per-output 容器，遍历全部摘除。\n- **unparentSurface(SurfaceWrapper*)**：一行 surface->setParent(nullptr)，通过 SurfaceContainer 的 friend 权限调用 SurfaceWrapper 的 private setParent 重载，同时清 QObject parent 与 QQuickItem parentItem，是配对解挂的正确入口。\n- **createNormalOutput / createCopyOutput 工厂内聚**：两个工厂开头对旧包装自动 detachLayerSurfaces，8 处 rewrap 调用点（进入/退出复制模式、恢复、禁用源折叠、热插拔源失败等）全部自动安全，新增 rewrap 点无遗漏风险。\n- **removeOutput() 重排序**：primary 切换移到 layer surface 搬迁之前。removeOutput 会把无输出绑定的全局 layer surface 搬到新主屏；若切换滞后，primaryOutput 仍指向正被删除的容器，全局表面会被发进待销毁容器导致崩溃。\n- **addSurfaceToContainer 守卫**：容器查找为空时由断言崩溃改为告警 + closed()。唯一剩余输出刚被移除、primary 仍指向它时容器必然为 null，优雅关闭优于崩溃。\n\n**净效果**：copy↔normal 包装交换 → detach（不关闭）→ removeOutput（空转）→ addOutput（re-enter）；只有真物理热拔插（onOutputRemoved）才关闭层表面，符合 layer-shell 规范。\n\n### ⑧ 协议回归测试（tests/protocols/treeland-virtual-output-desktop-v1）\nC 客户端跨 3 个协议（虚拟输出 + layer-shell + output-management）。layer surface 贯穿「进入复制 → 恢复 → 再进入 → 禁用 source 折叠」4 次包装交换，每步断言层表面存活、归属新包装、closed 事件计数保持 0；最终态断言 source 输出禁用、镜像输出转为普通输出并成为主屏。headless 后端不自产帧，测试通过强制渲染驱动 disable commit 完成。wlroots 对 NULL listener 直接 abort，每个协议回调均注册。\n\n---\n\n## 提交 3：feat(test-virtual-output): extend example client\n\n把旧的命令行工具（./test-virtual-output HDMI-A-1 VGA-1）扩展为 GUI 验证器，为复制模式/主屏/启停/分辨率/色彩管理提供可交互复现手段。\n\n- **wlroutputmanager.{h,cpp}（新增）**：基于 zwlr_output_manager_v1 的封装。ModeTracker/HeadTracker 按「声明-完成」模式收集 mode/head 数据（current_mode 用下标记录，事件到达顺序不保证，done 后再映射）；ConfigWatcher 把 succeeded/failed/cancelled 统一为结果信号（deleteLater 先发信号再释放，防止在回调里销毁自己）。applyConfig 必须对每个 head 重述状态——wlr 配置对象只改一个 head 会让其他 head 被隐式关闭，这是协议最容易踩的坑。done(serial) 触发 headsChanged，真实热插拔时 UI 自动刷新。\n- **virtualoutputmanager**：客户端 VERSION 提到 3 以接收 virtual_output_modified 事件；virtual_output_list 空数组容错（空列表是合法状态，必须发出空信号清残留映射）。\n- **virtualclient（重构）**：三张映射（组名→VirtualOutput、组名→成员屏、屏名→组名）替代旧「单当前虚拟输出」模型，支持多复制组与成员逆查。getWlOutput 每次现查——镜像切换会替换 wl_output 代理。updateMirrorGroup 创建/销毁 copyscreen_<screen> 组，wl_array 顺序强制 [被镜像屏, 当前屏] 以符合服务端复制语义。refreshScreenInfo 是所有事件汇集的唯一刷新点，blockSignals + 恢复选中防止控件信号级联回灌。分辨率/刷新率联动保留精确 mHz 值，避免 double→int 精度丢失导致 set_mode 匹配失败。亮度/色温经 treeland_color_control_v1（set + commit 模型、绑定在 wl_output 上），代理更换后销毁重建重绑。组列表对账以服务端为权威清理本地幽灵组。\n\n---\n\n## 提交 4：fix(virtual-output): emit empty outputs events for vanished groups\n\n客户端 VirtualOutput::outputs 事件处理：删除对空 outputs 数组的提前 return，改为空数组也走解析流程并无条件发出 outputsReceived（输出列表保持空）。\n\n**为什么**：服务端对「组已消失」的 get_virtual_output 返回合法的空 outputs 事件（提交 1 的 else 分支），这是协议语义中的「消失通知」而非异常。旧代码把空数组当「无事发生」直接丢弃，客户端观察不到组消失，镜像组映射残留。\n\n**解决**：VirtualClient::onVirtualOutputOutputs 收到空事件后清空该组映射并刷新 UI；updateMirrorGroup 的移除分支不再基于脏数据操作；与服务端 virtual_output_modified 通知双侧收敛，两侧状态一致。\n\n---\n\n## 测试\n\n- tests/protocols/treeland-virtual-output-desktop-v1：layer surface 贯穿 4 次包装交换，closed 计数保持 0（本地 PASS）。\n- treeland-virtual-output-manager-v1：协议套件验证重名组拒绝、消失组空事件、modified 通知。\n- examples/test_virtual_output：GUI 工具端到端驱动复制组创建/销毁、输出启停、主屏切换、分辨率与色彩控制。\n\n## PMS\n\nTASK-390751\n\n\n## Full review by copilot-pull-request-reviewer\n\n<!-- ccr-overview-v2 -->\n\n## Copilot review overview\n\n### 🟡 Changes recommended\n\nSeveral correctness issues remain in proxy lifetime and refresh handling, de-overlapped state persistence, and output-mode event ordering.\n\n*Get a fresh assessment by requesting another Copilot review.*\n\n**Review effort:** Lite  \n**Findings:** 1 <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-light.png\" alt=\"High severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> · 5 <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture>\n\n<details open>\n<summary><strong>Open (6)</strong></summary>\n\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/high-v2-light.png\" alt=\"High severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Guard stale proxy destruction from tearing down active copy mode](#discussion_r4059159212) · New\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Refresh existing groups after virtual output modification](#discussion_r4059159246) · New\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Explicitly destroy Wayland proxy before erasing vanished group](#discussion_r4059159263) · New\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Handle virtual output creation errors and roll back local maps](#discussion_r4059159278) · New\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Resolve current mode after all mode declarations arrive](#discussion_r4059159292) · New\n- <picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.svg\"><img src=\"https://github.githubassets.com/static/images/icons/copilot-code-review/medium-v2-light.png\" alt=\"Medium severity\" width=\"62\" height=\"18\" align=\"texttop\"></picture> [Update pending output states after de-overlapping positions](#discussion_r4059159315) · New\n</details>\n\n<details>\n<summary><strong>What changed in this PR</strong></summary>\n\nThis PR hardens virtual/copy output handling, preserves layer surfaces during output-wrapper replacement, extends protocol notifications, and adds an interactive validation client.\n\n**Changes:**\n- Pins output layouts and improves overlap, hotplug, copy-mode, cursor, and window recovery.\n- Detaches/re-attaches layer surfaces safely and adds regression coverage.\n- Upgrades virtual-output protocol handling and expands the GUI test client.\n\n| File | Description |\r\n|---|---|\r\n| `waylib/​src/​server/​kernel/​woutputlayout.*` | Adds output pinning. / 增加输出布局固定。 |\r\n| `src/​seat/​helper.*` | Implements robust copy-mode, restore, layout, and surface handling. / 完善复制模式、恢复、布局和窗口处理。 |\r\n| `src/​core/​{rootsurfacecontainer,layersurfacecontainer}.*` | Preserves layer surfaces across wrapper swaps. / 保持层表面跨包装交换存活。 |\r\n| `src/​surface/​surfacecontainer.*` | Adds safe surface unparenting. / 增加安全解挂逻辑。 |\r\n| `src/​output/​outputmanager.cpp` | Validates copy restoration and primary-output persistence. / 校验复制恢复和主屏持久化。 |\r\n| `src/​modules/​virtual-output/​*` | Adds protocol v3 notifications and lifecycle cleanup. / 增加 v3 通知和生命周期清理。 |\r\n| `tests/​protocols/​treeland-virtual-output-desktop-v1/​*` | Adds layer-surface/copy-mode regression coverage. / 增加层表面与复制模式回归测试。 |\r\n| `tests/​protocols/​INDEX.md` | Updates protocol coverage documentation. / 更新协议覆盖说明。 |\r\n| `examples/​test_virtual_output/​*` | Adds GUI output-management and virtual-output controls. / 增加 GUI 输出管理与虚拟输出控制。 |\n</details>\n\n---\n\n💡 Configure MCP servers for context-aware, tailored reviews. <a href=\"https://docs.github.com/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review?tool=webui#mcp-servers-and-agent-skills\" class=\"Link--inTextBlock\" target=\"_blank\" rel=\"noopener noreferrer\">Learn more in the docs.</a>\n## Full comment IC_kwDOFuYCn88AAAABS_F3qw\n\n<!-- Generated by sourcery-ai[bot]: start review_guide -->\n\n## Reviewer's Guide\n\nThe pull request turns the virtual-output example into a full interactive output-management client and expands the virtual-output protocol, while hardening compositor output topology transitions, layout positioning, cursor placement, primary-output persistence, and surface restoration across disable/enable and copy-mode changes.\n\n#### Sequence diagram for copy-source disable and restoration\n\n```mermaid\nsequenceDiagram\n    participant Client as OutputManagementClient\n    participant Helper\n    participant RootSurfaceContainer\n    participant OutputManager\n    participant Wlroots\n\n    Client->>Helper: onOutputTestOrApply(config)\n    Helper->>Helper: handleCopyModeSourceDisabled(disabledSource, requestedDisabled)\n    alt surviving mirrors remain\n        Helper->>Helper: promoteCopyOutputToSource(promotedMirror, surfaces, surfacesFrom, shouldRetarget)\n        Helper->>RootSurfaceContainer: setPrimaryOutput(normalSource)\n        Helper->>Helper: m_pendingOutputConfig.copyOutputIds = copyOutputIds\n    else no mirror remains\n        Helper->>Helper: convertCopyModeToExtension(disabledSource)\n    end\n    Helper->>Wlroots: commit output states\n    Wlroots-->>Helper: commit callback\n    Helper->>OutputManager: onOutputCommitFinished(config, success)\n    alt successful re-enable in extension mode\n        Helper->>Helper: surface->setPosition(savedPosition)\n        Helper->>RootSurfaceContainer: updateSurfaceOutputs(surface)\n    end\n```\n\n#### Sequence diagram for virtual-output updates and protocol notifications\n\n```mermaid\nsequenceDiagram\n    participant Client as WaylandClient\n    participant Manager as VirtualOutputManagerInterfaceV1\n    participant Binding as VirtualOutputInterfaceV1\n    participant Config as VirtualOutputConfig\n\n    Client->>Manager: create_virtual_output(resource, id, name, outputs)\n    Manager->>Config: storeVirtualOutput(name, outputs, virtualOutput)\n    Manager->>Manager: notifyVirtualOutputModified(name)\n    Manager-->>Client: send_virtual_output_modified(resource, name)\n\n    Client->>Manager: get_virtual_output(resource, name, id)\n    Manager->>Binding: createVirtualOutput(resource, id, name)\n    alt virtual output exists\n        Manager->>Config: backfill virtualOutput binding\n        Manager-->>Binding: sendOutputs(name, outputs)\n    else virtual output was removed\n        Manager-->>Binding: sendOutputs(name, empty outputs)\n    end\n\n    Manager->>Manager: updateVirtualOutput(name, outputs)\n    Manager->>Binding: sendOutputs(name, outputs)\n    Manager->>Manager: notifyVirtualOutputModified(name)\n    Client->>Manager: destroy(resource)\n    Manager->>Manager: wl_resource_destroy(resource)\n```\n\n#### Flow diagram for safe output layout restoration\n\n```mermaid\nflowchart TD\n    A[Output added or layout restored] --> B[Compute effective saved size]\n    B --> C{Saved position overlaps enabled output?}\n    C -- Yes --> D[Keep auto-added position]\n    C -- No --> E[\"layout.move(output, savedPos)\"]\n    D --> F[\"layout.pin(output)\"]\n    E --> F\n    F --> G[Output remains stable during sibling geometry changes]\n\n    H[Output-management apply] --> I{Restored states overlap?}\n    I -- Yes --> J[Reject restored topology]\n    I -- No --> K[Apply restored states]\n```\n\n### File-Level Changes\n\n| Change | Details | Files |\n| ------ | ------- | ----- |\n| Rebuild the virtual-output example into an interactive display-management client. | <ul><li>Add Treeland color-control and primary-output bindings.</li><li>Add wlroots output-management bindings for head discovery and configuration.</li><li>Replace command-line cloning with a Qt UI for screen selection, mirroring, enablement, geometry, mode, refresh, rotation, scale, brightness, color temperature, and primary-output control.</li><li>Track virtual-output groups and refresh state when the compositor reports modifications.</li></ul> | `examples/test_virtual_output/CMakeLists.txt`<br/>`examples/test_virtual_output/main.cpp`<br/>`examples/test_virtual_output/virtualclient.cpp`<br/>`examples/test_virtual_output/virtualclient.h`<br/>`examples/test_virtual_output/wlroutputmanager.cpp`<br/>`examples/test_virtual_output/wlroutputmanager.h` |\n| Extend the virtual-output protocol implementation to support lifecycle and modification notifications. | <ul><li>Upgrade the manager interface to version 3 and handle empty output lists safely.</li><li>Emit modification events when virtual-output groups are created, updated, or removed.</li><li>Implement manager-resource destruction and safely handle groups that disappear between listing and lookup.</li><li>Rebind newly requested virtual-output resources to current server-side configurations.</li></ul> | `examples/test_virtual_output/virtualoutputmanager.cpp`<br/>`examples/test_virtual_output/virtualoutputmanager.h`<br/>`src/modules/virtual-output/virtualoutputmanagerinterfacev1.cpp`<br/>`src/modules/virtual-output/virtualoutputmanagerinterfacev1.h` |\n| Make output layout and cursor behavior resilient to stale or changing output topology. | <ul><li>Pin automatically positioned outputs after placement to prevent wlroots reflow.</li><li>Avoid restoring saved positions that overlap enabled outputs and de-overlap extension-mode states.</li><li>Move the cursor to the closest remaining layout point when it leaves all outputs.</li><li>Ignore disabled outputs during cursor hit testing.</li></ul> | `src/core/rootsurfacecontainer.cpp`<br/>`src/core/rootsurfacecontainer.h`<br/>`src/seat/helper.cpp`<br/>`waylib/src/server/kernel/woutputlayout.cpp`<br/>`waylib/src/server/kernel/woutputlayout.h` |\n| Improve copy-mode transitions, output disable/enable handling, and persistence. | <ul><li>Promote surviving mirrors when a copy source is disabled, or collapse copy mode when insufficient mirrors remain.</li><li>Convert mirror outputs to normal outputs for extension-mode transitions and mixed topologies.</li><li>Persist rebuilt copy groups only after all output commits succeed.</li><li>Restore disabled-output surfaces and clear snapshots on re-enable.</li><li>Persist primary-output restoration and reject incomplete stale copy configurations.</li></ul> | `src/output/outputmanager.cpp`<br/>`src/seat/helper.cpp`<br/>`src/seat/helper.h` |\n\n---\n\n<details>\n<summary>Tips and commands</summary>\n\n#### Interacting with Sourcery\n\n- **Trigger a new review:** Comment `@sourcery-ai review` on the pull request.\n- **Continue discussions:** Reply directly to Sourcery's review comments.\n- **Generate a GitHub issue from a review comment:** Ask Sourcery to create an\n  issue from a review comment by replying to it. You can also reply to a\n  review comment with `@sourcery-ai issue` to create an issue from it.\n- **Generate a pull request title:** Write `@sourcery-ai` anywhere in the pull\n  request title to generate a title at any time. You can also comment\n  `@sourcery-ai title` on the pull request to (re-)generate the title at any time.\n- **Generate a pull request summary:** Write `@sourcery-ai summary` anywhere in\n  the pull request body to generate a PR summary at any time exactly where you\n  want it. You can also comment `@sourcery-ai summary` on the pull request to\n  (re-)generate the summary at any time.\n- **Generate reviewer's guide:** Comment `@sourcery-ai guide` on the pull\n  request to (re-)generate the reviewer's guide at any time.\n- **Resolve all Sourcery comments:** Comment `@sourcery-ai resolve` on the\n  pull request to resolve all Sourcery comments. Useful if you've already\n  addressed all the comments and don't want to see them anymore.\n- **Dismiss all Sourcery reviews:** Comment `@sourcery-ai dismiss` on the pull\n  request to dismiss all existing Sourcery reviews. Especially useful if you\n  want to start fresh with a new review - don't forget to comment\n  `@sourcery-ai review` to trigger a new review!\n\n#### Customizing Your Experience\n\nAccess your [dashboard](https://app.sourcery.ai) to:\n- Enable or disable review features such as the Sourcery-generated pull request\n  summary, the reviewer's guide, and others.\n- Change the review language.\n- Add, remove or edit custom review instructions.\n- Adjust other review settings.\n\n#### Getting Help\n\n- [Contact our support team](mailto:support@sourcery.ai) for questions or feedback.\n- Visit our [documentation](https://docs.sourcery.ai) for detailed guides and information.\n- Keep in touch with the Sourcery team by following us on [X/Twitter](https://x.com/SourceryAI), [LinkedIn](https://www.linkedin.com/company/sourcery-ai/) or [GitHub](https://github.com/sourcery-ai).\n\n</details>\n\n<!-- Generated by sourcery-ai[bot]: end review_guide -->\n## Full review thread comment 3948717932\n\n**issue (bug_risk):** Rebinding `m_configs[name].virtualOutput` to the newest proxy does not make the destroy cleanup proxy-specific: when the older proxy is later destroyed, its `beforeDestroy` handler unconditionally removes `m_configs[name]`, including the configuration now bound to the newer proxy.\n\n**Triggers:** When a client calls `get_virtual_output` for an existing group more than once and then destroys the older proxy.\n\n**Suggested fix:** Capture the proxy in the cleanup callback and remove the config only when `m_configs[name].virtualOutput` still points to that proxy.\n\n✅ Addressed in 576291721a650342d72350e5e14cd2c2044b35db: The cleanup callback now captures the specific proxy and removes the configuration only when that proxy is still the current binding, so destroying an older proxy cannot remove a newer binding.\n## Full review thread comment 4059159246\n\nEN: Refreshing only the virtual-output name list is insufficient for an existing group. The server stores one `virtualOutput` proxy per config and sends updated member lists only to that proxy; other clients receive `virtual_output_modified` but their existing names remain in `m_virtualOutputs`, so `onVirtualOutputListReceived()` skips `getVirtualOutput()` and their mirror mappings never update. Re-query/rebind each existing group named by the refreshed list (or otherwise fetch its outputs) when handling the modified event.\n中文：只刷新虚拟输出名称列表不足以更新已存在的复制组。服务端每个配置只保存一个 `virtualOutput` 代理，成员列表更新事件只发送给该代理；其他客户端虽然收到 `virtual_output_modified`，但名称仍在 `m_virtualOutputs` 中，因此 `onVirtualOutputListReceived()` 会跳过 `getVirtualOutput()`，镜像映射永远不会更新。处理 modified 事件时应重新查询/重绑定列表中的已有组，或以其他方式重新获取其 outputs。\n\n## Escalation commands\n- Full view: `gh pr view 1380 --repo linuxdeepin/treeland`\n- Complete diff: `gh pr diff 1380 --repo linuxdeepin/treeland`\n- Stored full document: use `get_search_content` with `offset` or `findText`.","error":null}]}